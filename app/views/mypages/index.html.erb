
<%= stylesheet_link_tag 'mypages', media: 'all', 'data-turbolinks-track': 'reload' %>

<script>
    function previewImage(obj)
    {
        var fileReader = new FileReader();
        fileReader.onload = (function() {
            document.getElementById('preview').src = fileReader.result;
        });
        fileReader.readAsDataURL(obj.files[0]);
        document.getElementById("image_text").innerHTML = "選択した画像";
    }
</script>



<div class="body">
<h1>アイコン変更</h1>

  <span>現在のアイコン</span>

  <div class="uploader">
  <div class="form_uploder">
    <%= form_tag("mypages/update", {multipart: true}) do %>
  <label>
    <input type="file" name="image" class="image_select"  accept='image/*' onchange="previewImage(this);"  required >
<% if @user.image.nil? %>
      <%= image_tag "/user_images/default_icon.png" , class: "user_icon_edit" %>
<% else %>
      <%= image_tag "/user_images/#{@user.image}",class: "user_icon_edit"%>
<% end %>
   </label><br>
      <p id="image_text">画像を選択してください</p>
      <p>
        <img id="preview" src="data:image/gif;base64,R0lGODlhAQABAAAAACH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" style="max-width:200px;">
      </p>

    <input type="submit" name="submit" value="保存"  class="btn btn-primary">

 <% end %>
    <a href="https://www.iloveimg.com/ja/resize-image/resize-jpg#resize-options,pixels">ここで300*300にしてね</a>
  </div>
</div>


</div>