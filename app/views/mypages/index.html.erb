
<h1>マイページ</h1>
<p>icon
  <%= current_user.nickname %>
  <%= current_user.userid%>

  <%= form_for(@users, url: mypages_path) do |f|%>
    <%= f.label "自己紹介" %>
    <%= f.text_area :message %>
    <%= f.label "タグ・趣味" %>
    <%= f.text_area :message %>
  <% end %>
</p>
